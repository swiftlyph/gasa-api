# Working conventions for this repo

- Commit per feature: split changes into one commit per logical feature
  rather than one large commit, even within a single session's work.
- Do not add a `Co-Authored-By` trailer (or any AI-attribution line) to
  commit messages in this repo.
